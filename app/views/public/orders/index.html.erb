<h1>注文履歴一覧</h1>
<% @orders.each do |order| %>
 <span><%= order.created_at %></span>
 <span><%= order.postal_code %></span>
 <span><%= order.address %>></span>
 <span><%= order.last_name + order.first_name %></span>
 <span><%= order.cart_items.all %></span>
 <% end %>