<h1>注文履歴詳細</h1>

<span>注文情報</span>
<a>注文日</a>
<span><%= @order.created_at %></span>

<a>配送先</a>
<span>〒<%= @order.postal_code %></span>
<span><%= @order.address %></span>
<span><%= @order.name %></span>

<a>支払い方法</a>
<span><%= @order.payment_method_i18n %></span>

<a>ステータス</a>
<span><%= @order.status_i18n %></span>

<span>請求情報</span>
 <span><%= @order.total_price.to_s(:delimited) %></span>
 <span><%= @order.postage %></span>
 <span><%=(@order.total_price + @order.postage).to_s(:delimited) %></span>

 <span>注文内容</span>
 <%  @order.order_details.each do |order_detail|  %>
 <%= order_detail.item.name %>
 <% end %>